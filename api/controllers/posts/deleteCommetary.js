// const Post = require("../../Post")

// const deletePost = async(req, res) => {
//   const post = await Post.findOne(req.params.id);
//   const commetaries = [];
//   const newPost = await Post.findByIdAndUpdate(req.params.id, {
//     comments: post.comments.filter((p) => p.)
//   })
// }